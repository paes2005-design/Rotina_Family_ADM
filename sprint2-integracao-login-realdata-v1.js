(async function(){
'use strict';
const VERSION='integracao-login-realdata-v1';
const API_ROOT='https://rotina-family-onesignal-scheduler.rotina-family-onesignal-scheduler.workers.dev';
const LOG_ENDPOINT=`${API_ROOT}/app-log`;
const ROLE_LABEL={adm_familia:'Administrador principal',adm_convidado:'Administrador convidado',master:'Master'};
const FULL_TO_SHORT={Domingo:'Dom',Segunda:'Seg',Terça:'Ter',Quarta:'Qua',Quinta:'Qui',Sexta:'Sex',Sábado:'Sáb'};
const SHORT_ORDER=['Dom','Seg','Ter','Qua','Qui','Sex','Sáb'];
const firebaseConfig={apiKey:'AIzaSyCP9odEV8TJGOM4lflHk64BbPyXXVjGcYg',authDomain:'sistema-de-metas-diarias.firebaseapp.com',projectId:'sistema-de-metas-diarias',storageBucket:'sistema-de-metas-diarias.firebasestorage.app',messagingSenderId:'576624564310',appId:'1:576624564310:web:fb2115a0c21659fefb83f7'};
const $=id=>document.getElementById(id);
const esc=(v='')=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const clean=v=>String(v??'').trim();
const clamp=(n,min=0,max=100)=>Math.min(max,Math.max(min,Number(n)||0));
const runtime={role:'',groupId:'',profiles:[],taskDocs:[],history:[],redemptions:[],analysisPeriod:'day',analysisRef:'',analysisParticipant:'all',manualLogin:false,loading:false};
const appState=window.RF_APP?.state;
if(!appState) throw new Error('Núcleo de Tarefas da Sprint 2 não carregou.');

const appMod=await import('https://www.gstatic.com/firebasejs/10.8.0/firebase-app.js');
const authMod=await import('https://www.gstatic.com/firebasejs/10.8.0/firebase-auth.js');
const fsMod=await import('https://www.gstatic.com/firebasejs/10.8.0/firebase-firestore.js');
const {initializeApp,deleteApp}=appMod;
const {getAuth,browserLocalPersistence,inMemoryPersistence,setPersistence,signInWithEmailAndPassword,signInWithCustomToken,signOut,onAuthStateChanged}=authMod;
const {initializeFirestore,persistentLocalCache,persistentMultipleTabManager,collection,query,where,getDocsFromServer}=fsMod;
const mainApp=initializeApp(firebaseConfig,'rotina-sprint2-integracao-realdata');
const auth=getAuth(mainApp);
await setPersistence(auth,browserLocalPersistence);
const db=initializeFirestore(mainApp,{localCache:persistentLocalCache({tabManager:persistentMultipleTabManager()})});

function browserFamily(){const ua=navigator.userAgent;if(/Edg\//.test(ua))return'Edge';if(/Firefox\//.test(ua))return'Firefox';if(/Chrome\//.test(ua))return'Chrome';if(/Safari\//.test(ua))return'Safari';return'Outro'}
async function emitLog(evento,details={},nivel='info'){
  const safe={};for(const [k,v] of Object.entries(details||{})){if(/email|senha|password|token|pin|nome|name/i.test(k)||typeof v==='object')continue;safe[k]=typeof v==='number'||typeof v==='boolean'?v:String(v).slice(0,100)}
  try{await fetch(LOG_ENDPOINT,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({events:[{aplicativo:'adm',versaoMonitor:3,evento,nivel,detalhes:{versao:VERSION,...safe},grupoId:runtime.groupId||'SPRINT2-PREVIEW',perfilId:'',sessaoId:'sprint2-realdata',clienteEm:new Date().toISOString(),pagina:'sprint2-integracao-login-realdata-v1.html',navegador:browserFamily(),online:navigator.onLine,visibilidade:document.visibilityState,instalado:false}]})})}catch(_){ }
}
function setLoginMessage(text='',bad=false){const el=$('loginMessage');if(!el)return;el.textContent=text;el.className=`auth-message${bad?' bad':''}`}
function setLoginBusy(on){$('loginButton').disabled=!!on;$('loginButton').textContent=on?'Validando e carregando…':'Entrar e carregar dados'}
function roleLabel(role){return ROLE_LABEL[role]||role||'Administrador'}
function normalizeGroupId(value){const v=clean(value).toUpperCase();if(!v)return'';if(/^CLI-\d+$/.test(v))return v;if(/^\d+$/.test(v))return`CLI-${v}`;return v}
function setMasterGroupMessage(text='',bad=false){const el=$('masterGroupMessage');if(!el)return;el.textContent=text;el.className=`master-group-message${bad?' bad':''}`}
function showMasterGroupPicker(){runtime.role='master';runtime.groupId='';$('loginForm').hidden=true;$('masterGroupPanel').hidden=false;$('masterGroupInput').value=normalizeGroupId(sessionStorage.getItem('rf-sprint2-master-selected-group'))||'CLI-4071';setMasterGroupMessage('Conta Master autenticada. Escolha o grupo que deseja visualizar.');setLoginMessage('')}
function resetAuthGate(){$('loginForm').hidden=false;$('masterGroupPanel').hidden=true;setMasterGroupMessage('')}
async function openMasterGroup(){const groupId=normalizeGroupId($('masterGroupInput').value);if(!/^CLI-\d+$/.test(groupId))return setMasterGroupMessage('Informe um grupo no formato CLI-4071 ou apenas 4071.',true);$('masterGroupInput').value=groupId;$('masterGroupLoad').disabled=true;setMasterGroupMessage(`Abrindo ${groupId}…`);try{sessionStorage.setItem('rf-sprint2-master-selected-group',groupId);await loadRealGroup('master',groupId);setMasterGroupMessage('');await emitLog('sprint2.realdata_master_grupo_aberto',{grupoIdSelecionado:groupId})}catch(e){setMasterGroupMessage(friendlyError(e),true)}finally{$('masterGroupLoad').disabled=false}}
async function logoutCurrent(){await emitLog('sprint2.realdata_logout',{papel:runtime.role});await signOut(auth).catch(()=>{});runtime.groupId='';runtime.role='';sessionStorage.removeItem('rf-sprint2-master-selected-group');document.body.classList.remove('rf-auth-ready');resetAuthGate();setLoginMessage('Sessão encerrada.')}
function isoDate(d){return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`}
function todayShort(){return SHORT_ORDER[new Date().getDay()]}
function todayFull(){return Object.keys(FULL_TO_SHORT).find(k=>FULL_TO_SHORT[k]===todayShort())||'Domingo'}
function pIcon(theme){return String(theme||'').toLowerCase().startsWith('masc')?'👦':'👧'}
function pointsOf(ex,task){const [a,b]=String(ex?.points||`0/${task?.points||0}`).split('/').map(Number);return{done:Number(a)||0,possible:Number(b)||Number(task?.points)||0}}
function execStatus(doc){const status=clean(doc?.status).toLowerCase();const faixa=clean(doc?.faixaAtraso).toLowerCase();if(status.includes('prazo'))return'ok';if(status.includes('atras')||faixa.includes('atraso'))return'late';if(status.includes('andamento'))return'running';return'pending'}
function taskIsActive(docs){return !docs.every(d=>d.ativa===false||d.ativo===false||d.active===false||clean(d.status).toLowerCase()==='inativa')}
function participantIdForDoc(doc){if(doc.perfilId&&runtime.profiles.some(p=>p.id===doc.perfilId))return doc.perfilId;const byName=runtime.profiles.find(p=>clean(p.nome).toLowerCase()===clean(doc.perfilNome).toLowerCase());return byName?.id||''}
function legacyGroupKey(doc,pid){return `${pid}::legacy::${clean(doc.nome)}::${clean(doc.horaSugeridaInicio)}::${clean(doc.horaSugeridaFim)}::${Number(doc.pontosMaximos)||0}::${Number(doc.tempoLimite)||0}`}
function recurrenceKey(doc,pid){return doc.tarefaGrupoId?`${pid}::${doc.tarefaGrupoId}`:legacyGroupKey(doc,pid)}
function recurrenceSignature(rec){return JSON.stringify([clean(rec.name),clean(rec.icon),rec.start,rec.end,rec.points,rec.tolerance,[...rec.days].sort().join(','),rec.status])}
function makeExecution(doc,points){return{start:clean(doc?.horarioInicio),end:clean(doc?.horarioTermino),status:execStatus(doc),points:`${Number(doc?.pontosGanhos)||0}/${Number(doc?.pontosMaximos)||points||0}`}}

function mapRealDataToState(){
  const participants=runtime.profiles.slice().sort((a,b)=>clean(a.nome).localeCompare(clean(b.nome),'pt-BR')).map(p=>({id:p.id,name:clean(p.nome)||'Integrante',theme:clean(p.sexo)||'Feminino',pinSet:!!(p.pinHash||p.pin)}));
  const groups=new Map();
  for(const doc of runtime.taskDocs){const pid=participantIdForDoc(doc);if(!pid)continue;const key=recurrenceKey(doc,pid);if(!groups.has(key))groups.set(key,{pid,docs:[]});groups.get(key).docs.push(doc)}
  const recurrences=[];
  for(const [key,g] of groups){const docs=g.docs;const base=docs[0]||{};const days=[...new Set(docs.map(d=>FULL_TO_SHORT[d.diaSemana]).filter(Boolean))];recurrences.push({sourceKey:key,pid:g.pid,docs,name:clean(base.nome)||'Tarefa',icon:clean(base.icone)||'✅',start:clean(base.horaSugeridaInicio)||'00:00',end:clean(base.horaSugeridaFim)||'00:00',points:Number(base.pontosMaximos)||0,tolerance:Number(base.tempoLimite)||0,days,status:taskIsActive(docs)?'active':'inactive',alarm:clean(base.alarme||base.horaAlarme),note:clean(base.observacao||base.nota)})}
  const buckets=new Map();for(const r of recurrences){const sig=recurrenceSignature(r);if(!buckets.has(sig))buckets.set(sig,[]);buckets.get(sig).push(r)}
  const tasks=[];const executions={};let nextId=1;const allPids=participants.map(p=>p.id);
  for(const bucket of buckets.values()){
    const uniquePids=[...new Set(bucket.map(r=>r.pid))];const collective=participants.length>1&&uniquePids.length===participants.length&&allPids.every(pid=>uniquePids.includes(pid))&&bucket.length===participants.length;
    if(collective){const ref=bucket[0];const id=nextId++;const task={id,name:ref.name,icon:ref.icon,start:ref.start,end:ref.end,points:ref.points,tolerance:ref.tolerance,days:ref.days,status:ref.status,alarm:ref.alarm,note:ref.note,collective:true,targets:[...allPids]};tasks.push(task);for(const r of bucket){const today=r.docs.find(d=>d.diaSemana===todayFull());executions[`${id}::${r.pid}`]=makeExecution(today,task.points)}}
    else{for(const r of bucket){const id=nextId++;const task={id,name:r.name,icon:r.icon,start:r.start,end:r.end,points:r.points,tolerance:r.tolerance,days:r.days,status:r.status,alarm:r.alarm,note:r.note,collective:false,targets:[r.pid]};tasks.push(task);const today=r.docs.find(d=>d.diaSemana===todayFull());executions[`${id}::${r.pid}`]=makeExecution(today,task.points)}}
  }
  appState.participants=participants;appState.tasks=tasks;appState.executions=executions;appState.taskParticipant=participants[0]?.id||'__ALL__';appState.taskSearch='';appState.taskFilter='all';appState.editingTask=null;appState.openTask=null;appState.nextTaskId=nextId+1;window.RF_APP.normalizeState();
}

window.rotinaSprint2BaseSnapshot=()=>({role:runtime.role,groupId:runtime.groupId,profiles:runtime.profiles.map(x=>({...x})),taskDocs:runtime.taskDocs.map(x=>({...x})),history:runtime.history.map(x=>({...x})),lastLoadedAt:Number(runtime.lastLoadedAt)||0});
window.rotinaSprint2SessionSnapshot=()=>({role:runtime.role,groupId:runtime.groupId});

function readOnlyToast(){window.RF_APP.toast('Modo leitura: primeiro valide os dados reais.');}
const coreToggleDetails=window.RF_APP.toggleTaskDetails.bind(window.RF_APP);
window.RF_APP.addTask=readOnlyToast;window.RF_APP.editTask=readOnlyToast;window.RF_APP.saveTask=readOnlyToast;window.RF_APP.removeTask=readOnlyToast;window.RF_APP.toggleTaskDay=readOnlyToast;window.RF_APP.setTaskDetail=readOnlyToast;
window.RF_APP.toggleTaskDetails=id=>{coreToggleDetails(id);requestAnimationFrame(lockTaskActions)};
function lockTaskActions(){document.querySelectorAll('#view-tarefas button[title="Editar"],#view-tarefas button[title="Excluir"]').forEach(b=>{b.disabled=true;b.classList.add('read-only')});document.querySelectorAll('#view-tarefas .expand .day,#view-tarefas .details-mobile .day,#view-tarefas .expand input,#view-tarefas .expand textarea,#view-tarefas .details-mobile input,#view-tarefas .details-mobile textarea').forEach(el=>el.disabled=true)}
function renderTasksSafe(){window.RF_APP.renderTasks();requestAnimationFrame(lockTaskActions)}

function todayRows(){const day=todayShort(),rows=[];for(const task of appState.tasks){if(task.status!=='active'||!task.days.includes(day))continue;for(const pid of task.targets||[]){const ex=appState.executions[`${task.id}::${pid}`]||{start:'',end:'',status:'pending',points:`0/${task.points}`};rows.push({task,pid,ex})}}return rows}
function participantName(pid){return appState.participants.find(p=>p.id===pid)?.name||pid}
function renderToday(){
  const rows=todayRows(),done=rows.filter(r=>['ok','late'].includes(r.ex.status)).length,running=rows.filter(r=>r.ex.status==='running').length,attention=rows.filter(r=>['pending','late'].includes(r.ex.status)).length,points=rows.reduce((s,r)=>s+pointsOf(r.ex,r.task).done,0),pct=rows.length?Math.round(done*100/rows.length):0;
  $('todayDateLabel').textContent=new Date().toLocaleDateString('pt-BR',{weekday:'long',day:'2-digit',month:'long'});$('todayExpected').textContent=rows.length;$('todayDone').textContent=done;$('todayRunning').textContent=running;$('todayAttention').textContent=attention;$('todayPoints').textContent=points;$('todayHeadline').textContent=`${done} de ${rows.length} tarefas concluídas`;$('todayPct').textContent=`${pct}%`;$('todayBar').style.width=`${pct}%`;
  const pending=rows.filter(r=>['pending','late'].includes(r.ex.status));$('actionList').innerHTML=pending.length?pending.slice(0,10).map(r=>`<button class="action-item" data-task-route><span class="action-icon">${r.ex.status==='late'?'⚠️':'⏳'}</span><span><b>${esc(participantName(r.pid))}: ${esc(r.task.name)}</b><small>${r.ex.status==='late'?'Concluída com atraso ou fora do prazo.':'Ainda pendente na agenda de hoje.'}</small></span><span class="action-arrow">›</span></button>`).join(''):'<div class="notice">Nenhuma pendência encontrada na agenda de hoje.</div>';
  $('actionList').querySelectorAll('[data-task-route]').forEach(b=>b.onclick=()=>navigate('tarefas'));
  $('personList').innerHTML=appState.participants.map(p=>{const mine=rows.filter(r=>r.pid===p.id),pdone=mine.filter(r=>['ok','late'].includes(r.ex.status)).length,ppts=mine.reduce((s,r)=>s+pointsOf(r.ex,r.task).done,0),ppct=mine.length?Math.round(pdone*100/mine.length):0;return`<button class="person-item" data-pid="${esc(p.id)}"><span class="person-avatar">${pIcon(p.theme)}</span><span><b>${esc(p.name)}</b><small>${pdone}/${mine.length} tarefas concluídas</small><span class="mini-track"><i style="width:${ppct}%"></i></span></span><span class="person-score">${ppts} pts</span></button>`}).join('')||'<div class="notice">Nenhum participante cadastrado no grupo.</div>';
  $('personList').querySelectorAll('[data-pid]').forEach(b=>b.onclick=()=>{appState.taskParticipant=b.dataset.pid;renderTasksSafe();navigate('tarefas')});
}

function dateFromIso(v){const [y,m,d]=String(v||isoDate(new Date())).split('-').map(Number);return new Date(y,m-1,d,12)}
function bounds(refIso,period){const ref=dateFromIso(refIso);if(period==='day')return{start:refIso,end:refIso};if(period==='week'){const s=new Date(ref);s.setDate(s.getDate()-s.getDay());const e=new Date(s);e.setDate(e.getDate()+6);return{start:isoDate(s),end:isoDate(e)}}if(period==='month')return{start:`${ref.getFullYear()}-${String(ref.getMonth()+1).padStart(2,'0')}-01`,end:isoDate(new Date(ref.getFullYear(),ref.getMonth()+1,0,12))};return{start:`${ref.getFullYear()}-01-01`,end:`${ref.getFullYear()}-12-31`}}
function historyPid(h){if(h.perfilId&&appState.participants.some(p=>p.id===h.perfilId))return h.perfilId;return appState.participants.find(p=>clean(p.name).toLowerCase()===clean(h.perfilNome).toLowerCase())?.id||''}
function selectedHistory(){const b=bounds(runtime.analysisRef,runtime.analysisPeriod);return runtime.history.filter(h=>clean(h.data)>=b.start&&clean(h.data)<=b.end&&(runtime.analysisParticipant==='all'||historyPid(h)===runtime.analysisParticipant))}
function selectedRedemptions(){const b=bounds(runtime.analysisRef,runtime.analysisPeriod);return runtime.redemptions.filter(r=>{const date=clean(r.decididoEm||r.criadoEm).slice(0,10);const approved=clean(r.status).toLowerCase()==='aprovado';const pid=r.perfilId||appState.participants.find(p=>clean(p.name).toLowerCase()===clean(r.perfilNome).toLowerCase())?.id||'';return approved&&date>=b.start&&date<=b.end&&(runtime.analysisParticipant==='all'||pid===runtime.analysisParticipant)})}
function countCurrentScheduleOccurrences(startIso,endIso,pid='all'){
  const start=dateFromIso(startIso),end=dateFromIso(endIso);let count=0,possible=0;for(let d=new Date(start);d<=end;d.setDate(d.getDate()+1)){const short=SHORT_ORDER[d.getDay()];for(const t of appState.tasks){if(t.status!=='active'||!t.days.includes(short))continue;const targets=(t.targets||[]).filter(id=>pid==='all'||id===pid);count+=targets.length;possible+=targets.length*(Number(t.points)||0)}}return{count,possible}}
function renderAnalysis(){
  const hist=selectedHistory(),b=bounds(runtime.analysisRef,runtime.analysisPeriod),sched=countCurrentScheduleOccurrences(b.start,b.end,runtime.analysisParticipant),points=hist.reduce((s,h)=>s+(Number(h.pontosGanhos)||0),0),redeemed=selectedRedemptions().reduce((s,r)=>s+(Number(r.pontos)||0),0),ontime=hist.filter(h=>clean(h.status).toLowerCase().includes('prazo')).length,completed=hist.length,use=sched.possible?Math.round(points*100/sched.possible):0,punct=completed?Math.round(ontime*100/completed):0,fulfill=sched.count?Math.round(completed*100/sched.count):0;
  $('kpiPoints').textContent=points;$('kpiUse').textContent=sched.possible?`${use}%`:'—';$('kpiPunctual').textContent=completed?`${punct}%`:'—';$('kpiFulfill').textContent=sched.count?`${fulfill}%`:'—';$('kpiRedeemed').textContent=redeemed;
  const totals=appState.participants.map(p=>({p,pts:runtime.history.filter(h=>{const date=clean(h.data),hp=historyPid(h);return date>=b.start&&date<=b.end&&hp===p.id}).reduce((s,h)=>s+(Number(h.pontosGanhos)||0),0)})).filter(x=>runtime.analysisParticipant==='all'||x.p.id===runtime.analysisParticipant);const max=Math.max(1,...totals.map(x=>x.pts));$('participantBars').innerHTML=totals.map(x=>`<div class="bar-row"><span class="bar-name">${esc(x.p.name)}</span><span class="bar-track"><i style="width:${clamp(x.pts*100/max)}%"></i></span><span class="bar-value">${x.pts}</span></div>`).join('')||'<div class="notice">Sem histórico no período.</div>';
  const today=todayRows().filter(r=>runtime.analysisParticipant==='all'||r.pid===runtime.analysisParticipant);$('statusOk').textContent=today.filter(r=>r.ex.status==='ok').length;$('statusLate').textContent=today.filter(r=>r.ex.status==='late').length;$('statusOpen').textContent=today.filter(r=>['pending','running'].includes(r.ex.status)).length;
  const top=[...totals].sort((a,b)=>b.pts-a.pts)[0],open=today.filter(r=>['pending','late','running'].includes(r.ex.status)).length;$('insights').innerHTML=`<div class="insight"><span class="ico">📊</span><div><b>${top?`${esc(top.p.name)} lidera o período com ${top.pts} pts.`:'Ainda sem pontos históricos no período.'}</b><p>Leitura baseada na coleção de histórico do grupo.</p></div></div><div class="insight"><span class="ico">🕒</span><div><b>${open} ocorrência(s) de hoje ainda pedem acompanhamento.</b><p>Usa o estado atual das tarefas da agenda de hoje.</p></div></div><div class="insight"><span class="ico">ℹ️</span><div><b>Percentuais de períodos usam a agenda atual como referência.</b><p>Histórico de alterações da agenda ainda não foi conectado neste checkpoint.</p></div></div>`;
}
function populateAnalysisParticipants(){const s=$('analysisParticipant'),keep=runtime.analysisParticipant;s.innerHTML='<option value="all">Todos</option>'+appState.participants.map(p=>`<option value="${esc(p.id)}">${esc(p.name)}</option>`).join('');s.value=[...s.options].some(o=>o.value===keep)?keep:'all';runtime.analysisParticipant=s.value}
function setHomePane(pane){const p=pane==='analysis'?'analysis':'today';document.querySelectorAll('.home-tab').forEach(b=>b.classList.toggle('active',b.dataset.pane===p));document.querySelectorAll('.home-pane').forEach(el=>el.classList.toggle('active',el.id===`pane-${p}`));sessionStorage.setItem('rf-sprint2-integration-realdata-pane',p);if(p==='analysis')renderAnalysis();else renderToday()}
function navigate(route){const target=route==='tarefas'?'tarefas':'inicio';document.querySelectorAll('.view').forEach(v=>v.classList.toggle('active',v.id===`view-${target}`));document.querySelectorAll('#mainNav [data-route]').forEach(b=>b.classList.toggle('active',b.dataset.route===target));history.replaceState(null,'',`#${target}`);if(target==='tarefas')renderTasksSafe();else renderToday();$('mainScroll').scrollTop=0;emitLog('sprint2.realdata_navegacao',{rota:target})}

async function workerSession(idToken){const c=new AbortController(),timeout=setTimeout(()=>c.abort(),9000);try{const r=await fetch(`${API_ROOT}/family-session/admin`,{method:'POST',cache:'no-store',signal:c.signal,headers:{authorization:`Bearer ${idToken}`,'content-type':'application/json'},body:'{}'});const data=await r.json().catch(()=>({}));if(!r.ok||!data.token){const e=new Error(data.error||`Falha HTTP ${r.status}`);e.status=r.status;throw e}return data}finally{clearTimeout(timeout)}}
async function tempAuth(){const app=initializeApp(firebaseConfig,`rotina-s2-temp-${Date.now()}-${Math.random().toString(36).slice(2)}`),a=getAuth(app);await setPersistence(a,inMemoryPersistence);return{app,auth:a}}
async function performLogin(email,password){const temp=await tempAuth();try{const cred=await signInWithEmailAndPassword(temp.auth,email,password),session=await workerSession(await cred.user.getIdToken(true));await signInWithCustomToken(auth,session.token);return{role:clean(session.papel),groupId:clean(session.grupoId).toUpperCase()}}finally{try{await signOut(temp.auth)}catch(_){ }try{await deleteApp(temp.app)}catch(_){ }}}
async function restoreSession(user){const tr=await user.getIdTokenResult();let role=clean(tr.claims?.papel),groupId=clean(tr.claims?.grupoId).toUpperCase();if(!['adm_familia','adm_convidado','master'].includes(role)||(!groupId&&role!=='master')){const s=await workerSession(await user.getIdToken(true));await signInWithCustomToken(auth,s.token);role=clean(s.papel);groupId=clean(s.grupoId).toUpperCase()}return{role,groupId}}
async function loadRealGroup(role,groupId){if(runtime.loading)return;runtime.loading=true;try{if(!groupId||groupId==='SISTEMA')throw new Error('Este checkpoint precisa de uma conta vinculada a um grupo familiar. Use um administrador do grupo para validar Início e Tarefas.');runtime.role=role;runtime.groupId=groupId;$('syncStatus').textContent='Lendo participantes, tarefas e histórico…';const started=performance.now();const [profilesSnap,tasksSnap,historySnap,redemptionsSnap]=await Promise.all([getDocsFromServer(query(collection(db,'perfis'),where('grupoId','==',groupId))),getDocsFromServer(query(collection(db,'tarefas'),where('grupoId','==',groupId))),getDocsFromServer(query(collection(db,'historico'),where('grupoId','==',groupId))),getDocsFromServer(query(collection(db,'resgates'),where('grupoId','==',groupId)))]);runtime.profiles=profilesSnap.docs.map(d=>({id:d.id,perfilId:d.data().perfilId||d.id,...d.data()}));runtime.taskDocs=tasksSnap.docs.map(d=>({id:d.id,...d.data()}));runtime.history=historySnap.docs.map(d=>({id:d.id,...d.data()}));runtime.redemptions=redemptionsSnap.docs.map(d=>({id:d.id,...d.data()}));runtime.lastLoadedAt=Date.now();mapRealDataToState();runtime.analysisRef=isoDate(new Date());$('refDate').value=runtime.analysisRef;populateAnalysisParticipants();$('topRole').textContent=roleLabel(role);$('topGroup').textContent=`Grupo ${groupId}`;$('syncStatus').textContent=`${runtime.profiles.length} participantes • ${runtime.taskDocs.length} registros de agenda • sincronizado agora`;document.body.classList.add('rf-auth-ready');renderTasksSafe();renderToday();renderAnalysis();setHomePane(sessionStorage.getItem('rf-sprint2-integration-realdata-pane')||'today');navigate(location.hash.slice(1)==='tarefas'?'tarefas':'inicio');await emitLog('sprint2.realdata_sync_sucesso',{participantes:runtime.profiles.length,tarefasDocs:runtime.taskDocs.length,historico:runtime.history.length,tempoMs:Math.round(performance.now()-started),papel:role});}catch(error){console.error('Integração realdata:',error);document.body.classList.remove('rf-auth-ready');if(role==='master')setMasterGroupMessage(error.message||'Não foi possível carregar os dados reais.',true);else setLoginMessage(error.message||'Não foi possível carregar os dados reais.',true);await emitLog('sprint2.realdata_sync_erro',{mensagem:String(error?.message||error).slice(0,80)},'error');throw error}finally{runtime.loading=false}}
function friendlyError(e){if(e?.code==='auth/invalid-credential'||e?.code==='auth/wrong-password'||e?.code==='auth/user-not-found')return'E-mail ou senha inválidos.';if(e?.code==='auth/network-request-failed')return'Falha de rede ao acessar o Firebase.';if(Number(e?.status)===401||Number(e?.status)===403)return'A conta autenticou no Firebase, mas não está autorizada no ADM.';return e?.message||'Não foi possível entrar.'}

$('loginForm').addEventListener('submit',async ev=>{ev.preventDefault();if(runtime.loading)return;const email=clean($('loginEmail').value).toLowerCase(),password=clean($('loginSenha').value);if(!email||!password)return setLoginMessage('Informe e-mail e senha.',true);runtime.manualLogin=true;setLoginBusy(true);setLoginMessage('Validando Firebase, Worker e grupo…');try{const session=await performLogin(email,password);$('loginSenha').value='';if(session.role==='master'&&!session.groupId){showMasterGroupPicker();await emitLog('sprint2.realdata_master_autenticado',{papel:session.role})}else{await loadRealGroup(session.role,session.groupId);setLoginMessage('');await emitLog('sprint2.realdata_login_sucesso',{papel:session.role})}}catch(e){console.warn('Login Sprint2 realdata:',e);await emitLog('sprint2.realdata_login_erro',{mensagem:String(e?.message||e).slice(0,80),codigo:String(e?.code||''),status:Number(e?.status)||0},'error');await signOut(auth).catch(()=>{});setLoginMessage(friendlyError(e),true)}finally{runtime.manualLogin=false;setLoginBusy(false)}});
$('togglePassword').onclick=()=>{const i=$('loginSenha'),show=i.type==='password';i.type=show?'text':'password';$('togglePassword').textContent=show?'Ocultar':'Mostrar'};
$('logoutButton').onclick=logoutCurrent;$('logoutTopButton').onclick=logoutCurrent;$('masterLogoutButton').onclick=logoutCurrent;$('masterGroupLoad').onclick=openMasterGroup;$('masterGroupInput').addEventListener('keydown',e=>{if(e.key==='Enter')openMasterGroup()});
document.querySelectorAll('#mainNav [data-route]').forEach(b=>b.addEventListener('click',()=>navigate(b.dataset.route)));
$('taskParticipant').addEventListener('change',e=>{appState.taskParticipant=e.target.value;appState.editingTask=null;appState.openTask=null;renderTasksSafe()});$('taskSearch').addEventListener('input',e=>{appState.taskSearch=e.target.value;renderTasksSafe()});$('taskFilter').addEventListener('change',e=>{appState.taskFilter=e.target.value;renderTasksSafe()});document.querySelectorAll('.home-tab').forEach(b=>b.onclick=()=>setHomePane(b.dataset.pane));$('periodTabs').querySelectorAll('button').forEach(b=>b.onclick=()=>{runtime.analysisPeriod=b.dataset.period;$('periodTabs').querySelectorAll('button').forEach(x=>x.classList.toggle('active',x===b));renderAnalysis()});$('refDate').onchange=e=>{runtime.analysisRef=e.target.value||isoDate(new Date());renderAnalysis()};$('analysisParticipant').onchange=e=>{runtime.analysisParticipant=e.target.value;renderAnalysis()};window.addEventListener('hashchange',()=>navigate(location.hash.slice(1)==='tarefas'?'tarefas':'inicio'));

onAuthStateChanged(auth,async user=>{if(!user){if(!runtime.manualLogin)document.body.classList.remove('rf-auth-ready');return}if(runtime.manualLogin||runtime.loading)return;try{setLoginBusy(true);setLoginMessage('Restaurando sessão e carregando o grupo…');const s=await restoreSession(user);if(s.role==='master'&&!s.groupId){const selected=normalizeGroupId(sessionStorage.getItem('rf-sprint2-master-selected-group'));if(selected){await loadRealGroup('master',selected);setLoginMessage('');await emitLog('sprint2.realdata_sessao_restaurada',{papel:s.role,grupoMaster:selected})}else{showMasterGroupPicker();await emitLog('sprint2.realdata_master_restaurado',{papel:s.role})}}else{await loadRealGroup(s.role,s.groupId);setLoginMessage('');await emitLog('sprint2.realdata_sessao_restaurada',{papel:s.role})}}catch(e){console.warn('Restauração Sprint2 realdata:',e);await emitLog('sprint2.realdata_restauracao_erro',{mensagem:String(e?.message||e).slice(0,80),codigo:String(e?.code||''),status:Number(e?.status)||0},'error');await signOut(auth).catch(()=>{});setLoginMessage(friendlyError(e),true)}finally{setLoginBusy(false)}});
})();